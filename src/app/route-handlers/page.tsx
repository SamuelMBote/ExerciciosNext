'use client';

import Login from '@/components/Login';
import React, { FormEventHandler } from 'react';

const PageRouteHandlers = () => {
  return (
    <section className="container mx-auto">
      <Login />
    </section>
  );
};

export default PageRouteHandlers;
